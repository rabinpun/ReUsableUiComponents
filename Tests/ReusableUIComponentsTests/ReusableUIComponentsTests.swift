    import XCTest
    @testable import ReusableUIComponents

    final class ReusableUIComponentsTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(ReusableUIComponents().text, "Hello, World!")
        }
    }
